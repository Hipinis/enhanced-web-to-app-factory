name: Production Factory Engine
on:
  repository_dispatch:
    types: [start_build]

jobs:
  build_android:
    runs-on: ubuntu-latest
    steps:
      - name: 检出图纸库
        uses: actions/checkout@v4

      - name: 配置 Node.js 和 Java 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: 自动生成 Android 骨架并注入核心配置
        run: |
          npm init -y
          npm install @capacitor/core @capacitor/cli @capacitor/android
          
          # 【修复问题2】：安装官方状态栏插件，专门解决顶部重叠问题
          npm install @capacitor/status-bar
          
          mkdir www && echo "<html><body style='background:#111;color:#fff;text-align:center;padding-top:50px;'>Loading...</body></html>" > www/index.html
          
          # 【修复问题2】：配置 StatusBar 的 overlaysWebView 为 false，让网页避开状态栏
          echo '{
            "appId": "com.reality.app",
            "appName": "${{ github.event.client_payload.app_name }}",
            "webDir": "www",
            "server": {
              "url": "${{ github.event.client_payload.target_url }}",
              "cleartext": true,
              "allowNavigation": ["*"]
            },
            "plugins": {
              "StatusBar": {
                "overlaysWebView": false
              }
            }
          }' > capacitor.config.json

          npx cap add android

      - name: 锻造专属 App 图标 (保持不变)
        run: |
          npm install -g @capacitor/assets
          sudo apt-get update
          sudo apt-get install -y imagemagick
          mkdir -p assets
          
          if [ -n "${{ github.event.client_payload.icon_base64 }}" ]; then
            echo "${{ github.event.client_payload.icon_base64 }}" | base64 -d > assets/icon_raw
          elif [ -n "${{ github.event.client_payload.icon_url }}" ]; then
            curl -L -q "${{ github.event.client_payload.icon_url }}" -o assets/icon_raw
          else
            curl -L -q "https://img.icons8.com/fluency/512/android-os.png" -o assets/icon_raw
          fi
          
          convert assets/icon_raw -resize 512x512 assets/icon.png
          cp assets/icon.png assets/splash.png
          npx @capacitor/assets generate --android

      - name: 终极底层手术 (独立加载十几万条广告策略 + UI悬浮球)
        env:
          PAYLOAD: ${{ toJSON(github.event.client_payload) }}
        run: |
          cat << 'EOF' > inject.py
          import os, json, urllib.request, base64

          payload = json.loads(os.environ['PAYLOAD'])

          # --- 1. 修复问题1：硬核屏蔽引擎 (硬编码加载你指定的三大权威规则库) ---
          target_ad_urls = [
              "https://filters.adtidy.org/extension/chromium/filters/11.txt",
              "https://filters.adtidy.org/extension/chromium/filters/2.txt",
              "https://easylist-downloads.adblockplus.org/easylistchina.txt"
          ]
          user_rules = payload.get('ad_rules', '')
          if user_rules:
              target_ad_urls.extend([u.strip() for u in user_rules.split(',') if u.strip()])

          ad_domains = set()
          for url in set(target_ad_urls):
              try:
                  req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
                  lines = urllib.request.urlopen(req, timeout=15).read().decode('utf-8').splitlines()
                  for line in lines:
                      line = line.strip()
                      if not line or line.startswith('!') or line.startswith('['): continue
                      # 仅精确提取带有 ||domain.com^ 格式的网络拦截核心域名
                      if line.startswith('||') and '^' in line:
                          domain = line[2:line.find('^')]
                          if '/' not in domain and '*' not in domain:
                              ad_domains.add(domain)
              except Exception as e:
                  print(f"Failed to fetch {url}: {e}")

          # 【核心突破】：将解析出的十几万条规则写成 txt 资产文件（完美绕过 Java 编译时的数组上限报错）
          os.makedirs('android/app/src/main/assets', exist_ok=True)
          with open('android/app/src/main/assets/adblock_rules.txt', 'w', encoding='utf-8') as f:
              f.write('\n'.join(ad_domains))

          # --- 2. 悬浮球与毛玻璃 UI (纯 JS 构建 - 完全保持上一个版本的满意代码) ---
          float_js = """
          (function(){
              if(document.getElementById('reality-fab')) return;

              var dialogOverlay = document.createElement('div');
              dialogOverlay.style.cssText = 'display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(0,0,0,0.5);backdrop-filter:blur(15px);-webkit-backdrop-filter:blur(15px);z-index:9999999;align-items:center;justify-content:center;opacity:0;transition:opacity 0.3s ease;';
              dialogOverlay.innerHTML = '<div style="background:rgba(255,255,255,0.15);border:1px solid rgba(255,255,255,0.3);padding:30px;border-radius:20px;text-align:center;box-shadow:0 10px 30px rgba(0,0,0,0.5);color:white;font-family:sans-serif;width:80%;max-width:300px;"><h2 style="margin:0 0 15px 0;font-size:20px;">退出应用</h2><p style="margin:0 0 25px 0;font-size:14px;opacity:0.8;">您确定要退出吗？</p><div style="display:flex;gap:15px;"><button id="btn-cancel-exit" style="flex:1;padding:12px;border:none;border-radius:10px;background:rgba(255,255,255,0.1);color:white;font-size:15px;cursor:pointer;">取消</button><button id="btn-confirm-exit" style="flex:1;padding:12px;border:none;border-radius:10px;background:#c0392b;color:white;font-size:15px;cursor:pointer;">退出</button></div></div>';
              document.body.appendChild(dialogOverlay);

              window.RealityApp = {
                  showExitDialog: function() {
                      dialogOverlay.style.display = 'flex';
                      setTimeout(function(){ dialogOverlay.style.opacity = '1'; }, 10);
                  },
                  hideExitDialog: function() {
                      dialogOverlay.style.opacity = '0';
                      setTimeout(function(){ dialogOverlay.style.display = 'none'; }, 300);
                  }
              };
              document.getElementById('btn-cancel-exit').onclick = window.RealityApp.hideExitDialog;
              document.getElementById('btn-confirm-exit').onclick = function() { AndroidNative.exitApp(); };

              var fab = document.createElement('div');
              fab.id = 'reality-fab';
              var fabIconUrl = '__FLOAT_ICON_URL__';
              var fabBase64 = '__FLOAT_ICON_BASE64__';
              if(fabBase64) { fab.innerHTML = '<img src="data:image/png;base64,' + fabBase64 + '" style="width:100%;height:100%;border-radius:50%;object-fit:cover;">'; }
              else if(fabIconUrl) { fab.innerHTML = '<img src="' + fabIconUrl + '" style="width:100%;height:100%;border-radius:50%;object-fit:cover;">'; }
              else { fab.innerHTML = '⚙️'; }
              
              fab.style.cssText = 'position:fixed;bottom:50px;right:20px;width:50px;height:50px;background:rgba(255,255,255,0.2);color:#fff;border-radius:25px;text-align:center;line-height:50px;font-size:24px;z-index:999998;box-shadow:0 4px 15px rgba(0,0,0,0.3);cursor:pointer;backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);border:1px solid rgba(255,255,255,0.1);display:flex;align-items:center;justify-content:center;overflow:hidden;';
              document.body.appendChild(fab);

              var menu = document.createElement('div');
              menu.style.cssText = 'display:none;position:fixed;bottom:110px;right:20px;background:rgba(0,0,0,0.7);border-radius:15px;padding:10px;z-index:999997;color:white;flex-direction:column;gap:10px;backdrop-filter:blur(15px);transform:scale(0.9);opacity:0;transition:all 0.2s;';
              
              var mHTML = '';
              mHTML += '<div id="fab-home" style="padding:10px 15px;background:rgba(255,255,255,0.1);border-radius:8px;text-align:center;cursor:pointer;font-size:14px;white-space:nowrap;">🏠 返回首页</div>';
              if('__FLOAT_TRANS__' === 'True') mHTML += '<div id="fab-trans" style="padding:10px 15px;background:rgba(255,255,255,0.1);border-radius:8px;text-align:center;cursor:pointer;font-size:14px;white-space:nowrap;">🌐 网页翻译</div>';
              if('__FLOAT_DARK__' === 'True') mHTML += '<div id="fab-dark" style="padding:10px 15px;background:rgba(255,255,255,0.1);border-radius:8px;text-align:center;cursor:pointer;font-size:14px;white-space:nowrap;">🌙 黑暗模式</div>';
              
              var cMenus = __CUSTOM_MENUS__;
              cMenus.forEach(function(item, idx) {
                  mHTML += '<div id="fab-custom-'+idx+'" data-url="'+item.url+'" style="padding:10px 15px;background:rgba(255,255,255,0.1);border-radius:8px;text-align:center;cursor:pointer;font-size:14px;white-space:nowrap;">🔗 '+item.label+'</div>';
              });
              
              if('__FLOAT_EXIT__' === 'True') mHTML += '<div id="fab-exit" style="padding:10px 15px;background:rgba(231,76,60,0.3);border-radius:8px;text-align:center;cursor:pointer;font-size:14px;color:#ff7675;">❌ 退出应用</div>';
              
              menu.innerHTML = mHTML;
              document.body.appendChild(menu);

              fab.onclick = function() {
                  if(menu.style.display === 'none') {
                      menu.style.display = 'flex'; setTimeout(function(){ menu.style.transform='scale(1)'; menu.style.opacity='1'; },10);
                  } else {
                      menu.style.transform='scale(0.9)'; menu.style.opacity='0'; setTimeout(function(){ menu.style.display='none'; },200);
                  }
              };

              if(document.getElementById('fab-home')) document.getElementById('fab-home').onclick = function() { window.location.href = '__HOME_URL__' || '__TARGET_URL__'; };
              if(document.getElementById('fab-trans')) document.getElementById('fab-trans').onclick = function() { window.location.href = "https://translate.google.com/translate?sl=auto&tl=zh-CN&u=" + encodeURIComponent(window.location.href); };
              if(document.getElementById('fab-dark')) document.getElementById('fab-dark').onclick = function() { document.documentElement.style.filter = document.documentElement.style.filter ? '' : 'invert(1) hue-rotate(180deg)'; };
              cMenus.forEach(function(item, idx) {
                  var el = document.getElementById('fab-custom-'+idx);
                  if(el) el.onclick = function() { window.location.href = this.getAttribute('data-url'); };
              });
              if(document.getElementById('fab-exit')) document.getElementById('fab-exit').onclick = function() { menu.style.display='none'; window.RealityApp.showExitDialog(); };
          })();
          """
          float_js = float_js.replace('__FLOAT_ICON_URL__', payload.get('float_icon_url', ''))
          float_js = float_js.replace('__FLOAT_ICON_BASE64__', payload.get('float_icon_base64', ''))
          float_js = float_js.replace('__FLOAT_TRANS__', str(payload.get('float_translate', True)))
          float_js = float_js.replace('__FLOAT_DARK__', str(payload.get('float_darkmode', True)))
          float_js = float_js.replace('__FLOAT_EXIT__', str(payload.get('float_exit', True)))
          float_js = float_js.replace('__HOME_URL__', payload.get('float_home_url', ''))
          float_js = float_js.replace('__TARGET_URL__', payload.get('target_url', ''))
          float_js = float_js.replace('__CUSTOM_MENUS__', json.dumps(payload.get('custom_menus', [])))
          float_js_b64 = base64.b64encode(float_js.encode('utf-8')).decode('utf-8')

          # --- 3. 安卓底层核心注入 (异步加载文本规则，防止卡死) ---
          java_code = """
          package com.reality.app;
          import android.os.Bundle;
          import android.webkit.WebView;
          import android.webkit.WebResourceRequest;
          import android.webkit.WebResourceResponse;
          import com.getcapacitor.BridgeActivity;
          import com.getcapacitor.BridgeWebViewClient;
          import java.util.HashSet;
          import java.io.BufferedReader;
          import java.io.InputStreamReader;
          import android.util.Base64;
          import android.webkit.JavascriptInterface;
          import android.net.Uri;

          public class MainActivity extends BridgeActivity {
              private HashSet<String> adBlockSet = new HashSet<>();
              private long lastBackPressTime = 0;

              @Override
              public void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  
                  if (__FULLSCREEN__) {
                      getWindow().setFlags(1024, 1024);
                  }

                  // 【核心修复】：在后台线程极速加载十几万条本地广告规则，完全不卡界面
                  new Thread(() -> {
                      try {
                          BufferedReader reader = new BufferedReader(new InputStreamReader(getAssets().open("adblock_rules.txt")));
                          String line;
                          while ((line = reader.readLine()) != null) {
                              adBlockSet.add(line.trim());
                          }
                          reader.close();
                      } catch (Exception e) {}
                  }).start();

                  WebView webView = this.bridge.getWebView();
                  webView.addJavascriptInterface(new Object() {
                      @JavascriptInterface
                      public void exitApp() { finish(); }
                  }, "AndroidNative");

                  webView.setWebViewClient(new BridgeWebViewClient(this.bridge) {
                      @Override
                      public WebResourceResponse shouldInterceptRequest(WebView view, WebResourceRequest request) {
                          Uri uri = request.getUrl();
                          if (uri != null && uri.getHost() != null) {
                              String host = uri.getHost();
                              String[] parts = host.split("\\\\.");
                              StringBuilder checkDomain = new StringBuilder();
                              // 极速子域名匹配算法 (如拦截目标是 perchance.org，请求是 ad.perchance.org，将会被精准拦截)
                              for (int i = parts.length - 1; i >= 0; i--) {
                                  if (checkDomain.length() > 0) checkDomain.insert(0, ".");
                                  checkDomain.insert(0, parts[i]);
                                  if (adBlockSet.contains(checkDomain.toString())) {
                                      return new WebResourceResponse("text/plain", "UTF-8", null); // 直接斩断请求，呈现干净的网页
                                  }
                              }
                          }
                          return super.shouldInterceptRequest(view, request);
                      }

                      @Override
                      public void onPageFinished(WebView view, String url) {
                          super.onPageFinished(view, url);
                          view.evaluateJavascript(new String(Base64.decode("__B64_JS__", Base64.DEFAULT)), null);
                      }
                  });
              }

              @Override
              public void onBackPressed() {
                  WebView webView = this.bridge.getWebView();
                  if (webView != null && webView.canGoBack()) {
                      webView.goBack();
                  } else {
                      long currentTime = System.currentTimeMillis();
                      if (currentTime - lastBackPressTime < 2000) {
                          webView.evaluateJavascript("if(window.RealityApp) { window.RealityApp.showExitDialog(); }", null);
                          lastBackPressTime = 0;
                      } else {
                          lastBackPressTime = currentTime;
                          android.widget.Toast.makeText(this, "再按一次唤出退出菜单", android.widget.Toast.LENGTH_SHORT).show();
                      }
                  }
              }
          }
          """
          java_code = java_code.replace('__FULLSCREEN__', str(payload.get('fullscreen', False)).lower())
          java_code = java_code.replace('__B64_JS__', float_js_b64)

          with open('android/app/src/main/java/com/reality/app/MainActivity.java', 'w') as f:
              f.write(java_code)
          EOF
          python3 inject.py

      - name: 锻造真正的 APK
        run: |
          cd android
          cat <<EOT >> app/build.gradle
          configurations.all {
              resolutionStrategy.eachDependency { DependencyResolveDetails details ->
                  def requested = details.requested
                  if (requested.group == 'org.jetbrains.kotlin' && requested.name == 'kotlin-stdlib-jdk8') {
                      details.useVersion '1.8.22'
                  }
              }
          }
          EOT
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: 交付成品
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.client_payload.app_name }}_Finished
          path: android/app/build/outputs/apk/debug/app-debug.apk
